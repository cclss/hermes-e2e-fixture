# hermes-e2e-fixture

Minimal fixture repository for Hermes E2E tests.

Used by `tools/e2e.sh` in [hermes-cloud](https://github.com/cclss/hermes-cloud) as the default test target when running Job/Run flow tests.
